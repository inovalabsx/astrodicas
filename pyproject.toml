[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "astrodicas"
version = "0.1.0"
description = "Ecossistema AstroDicas — conteúdo astrológico automático"
requires-python = ">=3.11"
dependencies = []
